# 🚀 راهنمای سریع اتصال به PostgreSQL

## فقط 3 گام ساده! ⚡

### گام 1️⃣: اجرای اسکریپت راه‌اندازی

در ترمینال این دستور را اجرا کنید:

```bash
./setup-database.sh
```

این اسکریپت به صورت خودکار:
- دیتابیس `irooni` را ایجاد می‌کند
- کاربر `irooni_user` را ایجاد می‌کند
- همه چیز را تنظیم می‌کند

**نکته:** ممکن است از شما رمز sudo بخواهد.

---

### گام 2️⃣: به‌روزرسانی فایل .env

بعد از اجرای اسکریپت، فایل `.env` را باز کنید و این خط را اضافه/تغییر دهید:

```env
DATABASE_URL="postgresql://irooni_user:irooni_pass_2024@localhost:5432/irooni?schema=public"
```

---

### گام 3️⃣: تست اتصال

```bash
# تولید Prisma Client
npm run db:generate

# اعمال schema به دیتابیس
npm run db:push
```

اگر خطایی نداشت، یعنی همه چیز درست است! ✅

---

## 🎯 تست سریع با Prisma Studio

```bash
npm run db:studio
```

این دستور یک صفحه وب باز می‌کند که می‌توانید دیتابیس را ببینید.

---

## ❓ اگر خطا گرفتید

### خطای "permission denied" برای اسکریپت:
```bash
chmod +x setup-database.sh
./setup-database.sh
```

### خطای "PostgreSQL service not running":
```bash
sudo systemctl start postgresql
```

### خطای "database does not exist":
اسکریپت را دوباره اجرا کنید:
```bash
./setup-database.sh
```

---

## ✅ بعد از اتصال موفق

حالا می‌توانید در کد از Prisma استفاده کنید:

```javascript
import { prisma } from '@/lib/prisma'

// مثال
const users = await prisma.user.findMany()
```

---

**برای راهنمای کامل‌تر، فایل `SETUP_DATABASE.md` را بخوانید.**

