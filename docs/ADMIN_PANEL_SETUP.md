# راهنمای راه‌اندازی پنل ادمین

## 📋 مراحل راه‌اندازی

### 1. به‌روزرسانی دیتابیس

ابتدا باید schema جدید را به دیتابیس اعمال کنید:

```bash
# Generate Prisma Client
npm run db:generate

# Push schema changes to database
npm run db:push
```

یا اگر می‌خواهید migration ایجاد کنید:

```bash
npm run db:migrate
```

### 2. تنظیم کاربر Admin

بعد از به‌روزرسانی schema، باید کاربر با شماره `09198718211` را به عنوان admin تنظیم کنید.

می‌توانید این کار را از طریق Prisma Studio انجام دهید:

```bash
npm run db:studio
```

یا مستقیماً از دیتابیس:

```sql
UPDATE "User" SET role = 'admin' WHERE phone = '09198718211';
```

**نکته:** اگر کاربر با این شماره وجود ندارد، ابتدا باید با این شماره در سایت ثبت‌نام کنید.

### 3. دسترسی به پنل ادمین

1. با شماره `09198718211` وارد سایت شوید
2. به آدرس `/admin` بروید
3. اگر دسترسی دارید، پنل ادمین نمایش داده می‌شود

## 🎯 ویژگی‌های پنل ادمین

### مدیریت محصولات

- **مشاهده لیست محصولات**: `/admin/products`
- **افزودن محصول جدید**: `/admin/products/new`
- **ویرایش محصول**: `/admin/products/[id]/edit`
- **حذف محصول**: از صفحه لیست محصولات

### فیلترها و جستجو

- جستجو بر اساس نام یا کد محصول
- فیلتر بر اساس دسته‌بندی
- صفحه‌بندی (20 محصول در هر صفحه)

## 🔒 امنیت

- فقط کاربر با شماره `09198718211` می‌تواند به پنل ادمین دسترسی داشته باشد
- تمام API routes محافظت شده‌اند
- در صورت عدم دسترسی، به صفحه login هدایت می‌شوید

## 📝 ساختار API

### GET `/api/admin/products`
دریافت لیست محصولات با pagination و فیلتر

**Query Parameters:**
- `page`: شماره صفحه (default: 1)
- `limit`: تعداد در هر صفحه (default: 20)
- `search`: جستجو در نام و کد
- `category`: فیلتر بر اساس دسته‌بندی

### POST `/api/admin/products`
ایجاد محصول جدید

**Body:**
```json
{
  "name": "نام محصول",
  "code": "کد-منحصر-به-فرد",
  "category": "tshirt",
  "subcategory": "short-sleeve",
  "image": "/banners/product.png",
  "images": ["/banners/product-2.png"],
  "price": 420000,
  "originalPrice": 520000,
  "sizes": ["S", "M", "L", "XL"],
  "colors": ["مشکی", "سفید"],
  "inStock": true,
  "material": "پنبه 100%",
  "description": "توضیحات محصول",
  "features": ["ویژگی 1", "ویژگی 2"],
  "sizeChart": [
    { "size": "L", "chest": "60", "length": "70" }
  ]
}
```

### GET `/api/admin/products/[id]`
دریافت یک محصول

### PUT `/api/admin/products/[id]`
به‌روزرسانی محصول

### DELETE `/api/admin/products/[id]`
حذف محصول

## 🚀 مراحل بعدی

برای توسعه بیشتر می‌توانید:

1. **مدیریت کاربران**: اضافه کردن بخش مدیریت کاربران
2. **مدیریت سفارشات**: اضافه کردن بخش مدیریت سفارشات
3. **آمار و گزارشات**: اضافه کردن داشبورد با آمار
4. **مدیریت بلاگ**: اضافه کردن بخش مدیریت مقالات
5. **آپلود تصویر**: اضافه کردن امکان آپلود تصویر به جای URL

## ⚠️ نکات مهم

- قبل از حذف محصولات، مطمئن شوید که در سفارشات استفاده نشده‌اند
- کد محصول باید منحصر به فرد باشد
- تمام فیلدهای الزامی باید پر شوند
- تصاویر باید در پوشه `public` قرار گیرند

